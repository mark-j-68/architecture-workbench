/**
 * Legacy M2 compatibility module.
 *
 * <p>The classes in {@code workbench-core} preserve the original sectioned
 * {@code ArchitectureModel} and early projection/validation prototypes. New
 * runtime boundaries must depend on {@code architecture-knowledge-graph}; the
 * sectioned model is retained only for migration, examples, and compatibility.
 */
@Deprecated
package com.architectureworkbench.core;
