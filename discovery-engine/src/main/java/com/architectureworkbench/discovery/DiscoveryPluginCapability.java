package com.architectureworkbench.discovery;

public enum DiscoveryPluginCapability {
    REGISTER_SOURCE,
    DETECT_REPOSITORY_STRUCTURE,
    DETECT_MAVEN_BUILD,
    DETECT_BUILD_MODULES,
    DETECT_DEPENDENCIES,
    DETECT_BUILD_PLUGINS
}
